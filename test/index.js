// Requires the tests files to run them
require('./vector')

require('./matrix')

require('./disambiguation')

require('./numeric')

require('./readme')

console.log('All tests successfully passed')
process.exit(0)
